# C3 — Components (overview)

Este directorio explica las opciones de organización de componentes del contenedor API: `AllProject` (por capas), `ByModule` (por dominio), `MVC` y `DDD`.

Ver `structure.md` y `variants/` para ejemplos y recomendaciones.
